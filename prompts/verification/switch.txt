你是一个 Logisim 自动化专家。请根据以下任务目标和电路列表，确定需要切换到的目标子电路，并输出对应的 switch_circuit 指令。

### 📋 任务上下文
- **任务名**: {task_name}
- **目标 (Goal)**: {goal}
- **建议目标子电路**: {target_subcircuit}
- **项目所有可用子电路**: {circuit_list}

### 🛠 输出格式
必须输出一个合法的 JSON 对象，包含 action 和 name 两个字段：
```json
{{ "action": "switch_circuit", "name": "选中的电路名称" }}
```

### ⚠️ 规则
1. 从“项目所有可用子电路”列表中选择最符合任务描述的电路。
2. 如果“建议目标子电路”在列表中存在且符合逻辑，优先选择它。
3. 请直接输出 JSON 对象，无需额外解释。
