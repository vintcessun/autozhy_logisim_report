# Gemini API 配置文件

[gemini]
# API 密钥
api_key = "YOUR_API_KEY_HERE"

# API 基准地址 (可选，用于中转或特定区域)
base_url = "YOUR_BASE_URL_HERE"

# 模型选型
model_pro = "gemini-3-flash" # 测试使用的是flash作为决策模型
model_flash = "gemini-3.1-flash-lite" # 使用flash-lite作为进行具体实践的模型
